from game.controller.GameController import GameController
# TODO: Add transitions for a better visual. Add score. Add reset button and its logic (VIEW and MODEL).

if __name__ == "__main__":
    controller = GameController()
    controller.run()